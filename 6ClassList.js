const titulo = document.querySelector(".titulo");

// let valor = titulo.classList.nombredelmetodo("parametro")

// add() - añade una clase
// remove() - remueve una clase
// item() - devuelve la clase del indice especificado
// contains() - verifica si ese elemnto posee o no, la clase especificada
// replace() - reemplaza una clase por otra
// toggle() - si no tiene la clase especificada, la agrega, si ya la 
// tiene, la elimina. Y tiene un parametro opcional que es true o false